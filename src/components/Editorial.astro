<section class="km-editorial">
  <div class="km-container">
    <div class="km-editorial-line reveal-on-scroll"></div>

    <h2 class="km-editorial-title reveal-on-scroll">
      DISEÑO, TECNOLOGÍA Y PERFORMANCE<br />
      PARA IMPULSAR TU NEGOCIO.
    </h2>

    <p class="km-editorial-text reveal-on-scroll">
      En KÜMERA construimos sitios web modernos, rápidos y orientados a conversión. Diseñamos
      experiencias claras que transforman visitantes en clientes y las potenciamos con campañas de
      Google Ads de alto rendimiento.
    </p>
  </div>
</section>

<style>
  /* ----------------------------------------- */
  /* FIX PARA QUE SE VEA                       */
  /* ----------------------------------------- */
  .km-editorial .km-container {
    color: #000 !important; /* fuerza el color */
  }

  .km-editorial * {
    color: inherit; /* mantiene el negro */
  }

  /* ----------------------------------------- */
  /* EDITORIAL — SOBRE FONDO BLANCO            */
  /* ----------------------------------------- */
  .km-editorial {
    padding: 8rem 0;
    background: #ffffff;
    color: #000000;
  }

  .km-editorial-line {
    width: 140px;
    height: 6px;
    background: #000;
    border-radius: 30px;
    margin-bottom: 3rem;
  }

  .km-editorial-title {
    font-family: var(--font-heading);
    font-size: clamp(3rem, 6vw, 5rem);
    line-height: 1.05;
    font-weight: 700;
    text-transform: uppercase;
    margin: 0 0 2.5rem 0;
    max-width: 22ch;
  }

  .km-editorial-text {
    font-family: var(--font-body);
    font-size: 1.2rem;
    line-height: 1.6;
    max-width: 60ch;
    color: #444;
    margin: 0;
  }

  /* ----------------------------------------- */
  /* MOBILE                                     */
  /* ----------------------------------------- */
  @media (max-width: 768px) {
    .km-editorial {
      padding: 5rem 0;
      text-align: left;
    }
    .km-editorial-title {
      font-size: 2.5rem;
      max-width: 100%;
    }
    .km-editorial-text {
      font-size: 1rem;
      max-width: 95%;
    }
  }

  /* ----------------------------------------- */
  /* REVEAL                                     */
  /* ----------------------------------------- */
  .reveal-on-scroll {
    opacity: 0;
    transform: translateY(40px);
    transition:
      opacity 0.9s cubic-bezier(0.25, 0.6, 0.35, 1),
      transform 0.9s cubic-bezier(0.25, 0.6, 0.35, 1);
  }

  .reveal-on-scroll.is-visible {
    opacity: 1;
    transform: translateY(0);
  }
</style>
