<section class="relative overflow-hidden bg-white">
  <div class="pointer-events-none absolute inset-0 flex justify-center">
    <div class="relative w-full max-w-[1120px]">
      <div class="absolute left-6 top-[18%] h-[2px] w-32 bg-black/60"></div>
      <div
        class="absolute right-[-10%] top-[62%] h-[2px] w-[140%] bg-orange-500/10 rotate-[-6deg] animate-line-drift-slow"
      >
      </div>
    </div>
  </div>

  <div class="relative z-10 mx-auto w-full max-w-[1120px] px-6 py-24 md:py-36">
    <div class="max-w-4xl">
      <p class="hero-eyebrow text-xs font-medium uppercase tracking-[0.38em] text-black/40">
        KÜMERA® — A Digital Performance & Creative Company
      </p>

      <h1
        class="pt-6 md:pt-8 hero-title font-['Space_Grotesk'] text-[3.4rem] leading-[1.02] tracking-tight text-black md:text-[5.2rem]"
      >
        <span class="word">Ayudamos</span>
        <span class="word">a</span>
        <span class="word bounce-word">dar</span>
        <span class="word bounce-word">vida</span>
        <span class="word">a</span>
        <span class="word highlight">tus ideas.</span>
      </h1>

      <p class="hero-sub py-1 max-w-[60ch] text-lg text-black/70 md:text-xl">
        Estrategia, diseño y campañas Google Ads pensadas para que tu negocio crezca con identidad,
        criterio y resultados reales.
      </p>

      <div class="hero-cta mt-14 flex flex-col gap-6 sm:flex-row">
        <a
          href="/contacto"
          class="inline-flex items-center justify-center rounded-full bg-black px-10 py-4 text-xs font-semibold uppercase tracking-[0.24em] text-white transition-all duration-300 hover:translate-y-[-2px] hover:opacity-90"
        >
          Hablemos de tu proyecto
        </a>

        <a
          href="/precios"
          class="inline-flex items-center justify-center rounded-full border border-black px-10 py-4 text-xs font-semibold uppercase tracking-[0.24em] text-black transition-all duration-300 hover:bg-black hover:text-white"
        >
          Ver planes y precios
        </a>
      </div>

      <p class="hero-foot py-1.5 text-sm text-black/45">
        Sitios web, Google Ads y ecommerce Shopify. Sin humo. Sin plantillas genéricas.
      </p>
    </div>
  </div>
</section>

<style>
  /* --- Typography animation (editorial build-in) --- */
  .hero-title {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35em;
  }

  .hero-title .word {
    opacity: 0;
    transform: translateY(22px) scale(0.96);
    animation: wordIn 0.9s cubic-bezier(0.22, 1, 0.36, 1) forwards;
  }

  .hero-title .bounce-word {
    animation-name: wordIn, softBounce;
    animation-duration: 0.9s, 1.4s;
    animation-delay: inherit, 0.95s;
    animation-timing-function: cubic-bezier(0.22, 1, 0.36, 1), cubic-bezier(0.34, 1.56, 0.64, 1);
    animation-fill-mode: forwards;
  }

  .hero-title .word:nth-child(1) {
    animation-delay: 0.05s;
  }
  .hero-title .word:nth-child(2) {
    animation-delay: 0.12s;
  }
  .hero-title .word:nth-child(3) {
    animation-delay: 0.18s;
  }
  .hero-title .word:nth-child(4) {
    animation-delay: 0.26s;
  }
  .hero-title .word:nth-child(5) {
    animation-delay: 0.34s;
  }
  .hero-title .word:nth-child(6) {
    animation-delay: 0.42s;
  }
  .hero-title .word:nth-child(7) {
    animation-delay: 0.5s;
  }

  .hero-title .highlight {
    background: black;
    color: white;
    padding: 0.1em 0.4em;
    display: inline-block;
  }

  .hero-eyebrow,
  .hero-sub,
  .hero-cta,
  .hero-foot {
    opacity: 0;
    transform: translateY(14px);
    animation: fadeUp 0.8s cubic-bezier(0.22, 1, 0.36, 1) forwards;
  }

  .hero-eyebrow {
    animation-delay: 0.15s;
  }
  .hero-sub {
    animation-delay: 0.65s;
  }
  .hero-cta {
    animation-delay: 0.75s;
  }
  .hero-foot {
    animation-delay: 0.9s;
  }

  @keyframes wordIn {
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  @keyframes softBounce {
    0% {
      transform: translateY(0) scale(1);
    }
    30% {
      transform: translateY(-6px) scale(1.02);
    }
    55% {
      transform: translateY(2px) scale(0.995);
    }
    75% {
      transform: translateY(-2px) scale(1.005);
    }
    100% {
      transform: translateY(0) scale(1);
    }
  }

  @keyframes fadeUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes lineDrift {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(-120px);
    }
  }

  .animate-line-drift-slow {
    animation: lineDrift 32s linear infinite;
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-title .word,
    .hero-title .bounce-word,
    .hero-eyebrow,
    .hero-sub,
    .hero-cta,
    .hero-foot,
    .animate-line-drift-slow {
      animation: none !important;
      opacity: 1;
      transform: none;
    }
  }
</style>
