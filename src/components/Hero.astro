<section class="hero">
  <div class="hero-inner">
    <div class="hero-line reveal-up"></div>

    <h1 class="hero-title reveal-up" style="animation-delay: .05s;">
      CONSTRUIMOS LO QUE <span>otros no pueden</span>.
    </h1>

    <p class="hero-sub reveal-up" style="animation-delay: .15s;">
      KUMERA® — A DIGITAL PERFORMANCE & CREATIVE COMPANY
    </p>

    <a href="#contacto" class="hero-cta reveal-up" style="animation-delay: .25s;">
      ➜ HABLEMOS DE TU PROYECTO
    </a>
  </div>
</section>

<style>
  /* -------------------------------------------------- */
  /* DESKTOP BASE */
  /* -------------------------------------------------- */

  .hero {
    background: var(--color-bg);
    padding-top: 10rem;
    padding-bottom: 10rem;
    display: flex;
    justify-content: center;
    text-align: center;
  }

  .hero-inner {
    max-width: 1000px;
    width: 100%;
    padding: 0 var(--space-lg);
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .hero-line {
    width: 180px;
    height: 6px;
    background: var(--color-text);
    border-radius: 30px;
    margin-bottom: 3rem;
  }

  .hero-title {
    font-family: var(--font-heading);
    font-size: clamp(4rem, 7vw, 6rem); /* MÁS GRANDE */
    font-weight: 700;
    line-height: 1.05; /* MÁS EDITORIAL */
    letter-spacing: -0.03em;
    margin-bottom: 3rem;
  }

  .hero-title span {
    background: var(--color-text);
    color: var(--color-bg);
    padding: 0 0.55rem;
    border-radius: 6px;
    transform: skew(-6deg);
    display: inline-block;
  }

  .hero-sub {
    font-size: 1rem;
    letter-spacing: 0.17em; /* MÁS ESPACIADO EDITORIAL */
    color: var(--color-muted);
    text-transform: uppercase;
    margin-bottom: 3rem;
    max-width: 60ch;
  }

  .hero-cta {
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border-bottom: 2px solid var(--color-text);
    padding-bottom: 0.35rem;
    color: var(--color-text);
    transition:
      opacity 0.25s ease,
      transform 0.25s ease;
  }

  .hero-cta:hover {
    opacity: 0.75;
    transform: translateY(-3px);
  }

  /* -------------------------------------------------- */
  /* ANIMACIÓN */
  /* -------------------------------------------------- */

  .reveal-up {
    opacity: 0;
    transform: translateY(20px);
    animation: revealUp 0.9s cubic-bezier(0.25, 0.6, 0.35, 1) forwards;
  }

  @keyframes revealUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* -------------------------------------------------- */
  /* MOBILE VERSION — ULTRA EDITORIAL, FULL-SCREEN */
  /* -------------------------------------------------- */
  @media (max-width: 768px) {
    .hero {
      height: 100vh;
      padding-top: 5rem; /* espacio desde header */
      padding-bottom: 4rem;
      text-align: center;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .hero-inner {
      max-width: 95%;
      padding: 0 1.2rem;
    }

    .hero-line {
      width: 130px;
      height: 5px;
      margin-bottom: 2rem;
      align-self: flex-start;
    }

    .hero-title {
      font-size: 3.5rem; /* ← MÁS GRANDE AÚN */
      line-height: 1.03; /* ← MÁS PEGADO, MÁS EDITORIAL */
      margin-bottom: 2rem;
      max-width: none; /* ← SIN LÍMITE */
    }

    .hero-title span {
      padding: 0 0.35rem;
    }

    .hero-sub {
      font-size: 0.78rem;
      letter-spacing: 0.2em; /* MÁS EDITORIAL AÚN */
      margin-bottom: 2rem;
    }

    .hero-cta {
      font-size: 0.95rem;
      border-width: 2px;
      padding-bottom: 0.35rem;
    }
  }

  /* -------------------------------------------------- */
  /* BACKGROUND NOISE ANIMATION — ULTRA SUTIL (APPLE)   */
  /* -------------------------------------------------- */

  .hero {
    position: relative;
    overflow: hidden; /* noise stays inside */
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;

    /* Noise via multiple tiny gradients */
    background-image: radial-gradient(circle, rgba(0, 0, 0, 0.04) 1px, transparent 1px),
      radial-gradient(circle, rgba(0, 0, 0, 0.03) 1px, transparent 1px),
      radial-gradient(circle, rgba(0, 0, 0, 0.02) 1px, transparent 1px);

    background-size:
      3px 3px,
      4px 4px,
      5px 5px;

    opacity: 0.35; /* fuerza del noise */
    animation: noiseMove 14s infinite linear;
  }

  @keyframes noiseMove {
    0% {
      transform: translate(0, 0);
    }
    50% {
      transform: translate(2px, 1px);
    }
    100% {
      transform: translate(0, 0);
    }
  }
</style>
