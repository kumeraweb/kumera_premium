<section class="km-cta">
  <div class="km-container">
    <div class="km-cta-line reveal-on-scroll"></div>

    <h2 class="km-cta-title reveal-on-scroll">
      ¿LISTO PARA IMPULSAR<br />TU NEGOCIO?
    </h2>

    <p class="km-cta-text reveal-on-scroll">
      Diseñamos sitios web modernos y campañas de Google Ads orientadas a resultados reales.
      Conversemos tu idea y la llevamos a un nivel superior.
    </p>

    <a href="https://wa.me/569XXXXXXXX" target="_blank" class="km-cta-btn reveal-on-scroll">
      ➜ INICIAR PROYECTO
    </a>
  </div>
</section>

<style>
  :root {
    --color-brand: #0f1d2e; /* azul petróleo premium */
  }

  .km-cta {
    padding: 10rem 0;
    background: var(--color-brand);
    color: #ffffff;
  }

  .km-cta-line {
    width: 120px;
    height: 5px;
    background: #fff;
    border-radius: 30px;
    margin-bottom: 3rem;
  }

  .km-cta-title {
    font-family: var(--font-heading);
    font-size: clamp(3rem, 6vw, 5rem);
    font-weight: 700;
    line-height: 1.05;
    text-transform: uppercase;
    margin-bottom: 2.5rem;
    max-width: 22ch;
    color: #fff; /* ← FIX */
  }

  .km-cta-text {
    font-family: var(--font-body);
    font-size: 1.2rem;
    max-width: 55ch;
    color: #e3e3e3;
    margin-bottom: 3rem;
    line-height: 1.6;
  }

  .km-cta-btn {
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    padding-bottom: 0.35rem;
    border-bottom: 2px solid #fff;
    color: #fff; /* ← FIX */
    transition:
      opacity 0.25s ease,
      transform 0.25s ease;
  }

  .km-cta-btn:hover {
    opacity: 0.75;
    transform: translateX(6px);
  }
</style>
