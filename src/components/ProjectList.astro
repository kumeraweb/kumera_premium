---
import { projects } from '../data/projects'
import ProjectItem from './ProjectItem.astro'

const showProjects = false
---

<section id="proyectos" class="project-list">
  {
    showProjects ? (
      projects.map((p) => <ProjectItem project={p} />)
    ) : (
      <p class="coming-soon">🚧 Próximamente: casos de estudio y proyectos seleccionados</p>
    )
  }
</section>

<style>
  .project-list {
    padding: 4rem 0;
    max-width: 900px;
    margin: 0 auto;
  }

  @media (max-width: 800px) {
    .project-list {
      padding: 3rem 1.6rem;
    }
  }

  .coming-soon {
    text-align: center;
    font-size: 1.4rem;
    opacity: 0.6;
    padding: 4rem 0;
  }
</style>
